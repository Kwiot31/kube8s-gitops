apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "my-app.fullname" . }}-index
  labels:
    {{- include "my-app.labels" . | nindent 4 }}
data:
  index.html: |
{{ .Files.Get "index.html" | indent 4 }}
